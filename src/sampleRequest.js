// let url = baseUrl + path + queryParams;
// let timestamp = new Date().toISOString().substr(0, 19) + "+0000";
// let signature = calculateSignature(apiKey, apiSecret, path, body, timestamp);

// return axios.get(url, {
//     headers: {
//         'ss-api-key': this.apiKey,
//         'ss-request-signature': signature,
//         'ss-request-timestamp': timestamp
//     }
// });
